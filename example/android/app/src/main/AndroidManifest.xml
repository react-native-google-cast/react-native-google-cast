<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Google Cast: the library OptionsProvider (v4-parity notifications,
           image picker, expanded controller). Mirrors the Expo plugin output
           for receiverAppId "CC1AD845" (explicit here to exercise the
           meta-data read path); NitroExpandedControllerActivity is
           auto-registered by the library manifest — no manual <activity>
           needed. -->
      <meta-data
        android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
        android:value="com.margelo.nitro.googlecast.NitroCastOptionsProvider" />
      <meta-data
        android:name="com.margelo.nitro.googlecast.RECEIVER_APPLICATION_ID"
        android:value="CC1AD845" />
    </application>
</manifest>
