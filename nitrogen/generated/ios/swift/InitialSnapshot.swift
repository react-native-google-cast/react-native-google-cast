///
/// InitialSnapshot.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `InitialSnapshot`, backed by a C++ struct.
 */
public typealias InitialSnapshot = margelo.nitro.googlecast.InitialSnapshot

public extension InitialSnapshot {
  private typealias bridge = margelo.nitro.googlecast.bridge.swift

  /**
   * Create a new instance of `InitialSnapshot`.
   */
  init(castState: CastState, playServicesState: PlayServicesState, devices: [Device], currentSession: SessionInfo?) {
    self.init(castState, playServicesState, { () -> bridge.std__vector_Device_ in
      var __vector = bridge.create_std__vector_Device_(devices.count)
      for __item in devices {
        __vector.push_back(__item)
      }
      return __vector
    }(), { () -> bridge.std__optional_SessionInfo_ in
      if let __unwrappedValue = currentSession {
        return bridge.create_std__optional_SessionInfo_(__unwrappedValue)
      } else {
        return .init()
      }
    }())
  }

  @inline(__always)
  var castState: CastState {
    return self.__castState
  }
  
  @inline(__always)
  var playServicesState: PlayServicesState {
    return self.__playServicesState
  }
  
  @inline(__always)
  var devices: [Device] {
    return self.__devices.map({ __item in __item })
  }
  
  @inline(__always)
  var currentSession: SessionInfo? {
    return self.__currentSession.value
  }
}
