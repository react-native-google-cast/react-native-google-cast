///
/// Device.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `Device`, backed by a C++ struct.
 */
public typealias Device = margelo.nitro.googlecast.Device

public extension Device {
  private typealias bridge = margelo.nitro.googlecast.bridge.swift

  /**
   * Create a new instance of `Device`.
   */
  init(capabilities: [DeviceCapability], deviceId: String, deviceVersion: String, friendlyName: String, icons: [WebImage], ipAddress: String, isOnLocalNetwork: Bool?, modelName: String) {
    self.init({ () -> bridge.std__vector_DeviceCapability_ in
      var __vector = bridge.create_std__vector_DeviceCapability_(capabilities.count)
      for __item in capabilities {
        __vector.push_back(__item)
      }
      return __vector
    }(), std.string(deviceId), std.string(deviceVersion), std.string(friendlyName), { () -> bridge.std__vector_WebImage_ in
      var __vector = bridge.create_std__vector_WebImage_(icons.count)
      for __item in icons {
        __vector.push_back(__item)
      }
      return __vector
    }(), std.string(ipAddress), { () -> bridge.std__optional_bool_ in
      if let __unwrappedValue = isOnLocalNetwork {
        return bridge.create_std__optional_bool_(__unwrappedValue)
      } else {
        return .init()
      }
    }(), std.string(modelName))
  }

  @inline(__always)
  var capabilities: [DeviceCapability] {
    return self.__capabilities.map({ __item in __item })
  }
  
  @inline(__always)
  var deviceId: String {
    return String(self.__deviceId)
  }
  
  @inline(__always)
  var deviceVersion: String {
    return String(self.__deviceVersion)
  }
  
  @inline(__always)
  var friendlyName: String {
    return String(self.__friendlyName)
  }
  
  @inline(__always)
  var icons: [WebImage] {
    return self.__icons.map({ __item in __item })
  }
  
  @inline(__always)
  var ipAddress: String {
    return String(self.__ipAddress)
  }
  
  @inline(__always)
  var isOnLocalNetwork: Bool? {
    return { () -> Bool? in
      if bridge.has_value_std__optional_bool_(self.__isOnLocalNetwork) {
        let __unwrapped = bridge.get_std__optional_bool_(self.__isOnLocalNetwork)
        return __unwrapped
      } else {
        return nil
      }
    }()
  }
  
  @inline(__always)
  var modelName: String {
    return String(self.__modelName)
  }
}
