///
/// HybridCastTransportSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `CastState` to properly resolve imports.
namespace margelo::nitro::googlecast { enum class CastState; }
// Forward declaration of `ListenerSubscription` to properly resolve imports.
namespace margelo::nitro::googlecast { struct ListenerSubscription; }

#include "CastState.hpp"
#include "ListenerSubscription.hpp"
#include <functional>

namespace margelo::nitro::googlecast {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `CastTransport`
   * Inherit this class to create instances of `HybridCastTransportSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridCastTransport: public HybridCastTransportSpec {
   * public:
   *   HybridCastTransport(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridCastTransportSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridCastTransportSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridCastTransportSpec() override = default;

    public:
      // Properties
      virtual bool getIsAvailable() = 0;

    public:
      // Methods
      virtual CastState getCastState() = 0;
      virtual ListenerSubscription addCastStateListener(const std::function<void(CastState /* state */)>& listener) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "CastTransport";
  };

} // namespace margelo::nitro::googlecast
