///
/// PlayServicesState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::googlecast {

  /**
   * An enum which can be represented as a JavaScript union (PlayServicesState).
   */
  enum class PlayServicesState {
    SUCCESS      SWIFT_NAME(success) = 0,
    MISSING      SWIFT_NAME(missing) = 1,
    UPDATING      SWIFT_NAME(updating) = 2,
    UPDATEREQUIRED      SWIFT_NAME(updaterequired) = 3,
    DISABLED      SWIFT_NAME(disabled) = 4,
    INVALID      SWIFT_NAME(invalid) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::googlecast

namespace margelo::nitro {

  // C++ PlayServicesState <> JS PlayServicesState (union)
  template <>
  struct JSIConverter<margelo::nitro::googlecast::PlayServicesState> final {
    static inline margelo::nitro::googlecast::PlayServicesState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("success"): return margelo::nitro::googlecast::PlayServicesState::SUCCESS;
        case hashString("missing"): return margelo::nitro::googlecast::PlayServicesState::MISSING;
        case hashString("updating"): return margelo::nitro::googlecast::PlayServicesState::UPDATING;
        case hashString("updateRequired"): return margelo::nitro::googlecast::PlayServicesState::UPDATEREQUIRED;
        case hashString("disabled"): return margelo::nitro::googlecast::PlayServicesState::DISABLED;
        case hashString("invalid"): return margelo::nitro::googlecast::PlayServicesState::INVALID;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum PlayServicesState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::googlecast::PlayServicesState arg) {
      switch (arg) {
        case margelo::nitro::googlecast::PlayServicesState::SUCCESS: return JSIConverter<std::string>::toJSI(runtime, "success");
        case margelo::nitro::googlecast::PlayServicesState::MISSING: return JSIConverter<std::string>::toJSI(runtime, "missing");
        case margelo::nitro::googlecast::PlayServicesState::UPDATING: return JSIConverter<std::string>::toJSI(runtime, "updating");
        case margelo::nitro::googlecast::PlayServicesState::UPDATEREQUIRED: return JSIConverter<std::string>::toJSI(runtime, "updateRequired");
        case margelo::nitro::googlecast::PlayServicesState::DISABLED: return JSIConverter<std::string>::toJSI(runtime, "disabled");
        case margelo::nitro::googlecast::PlayServicesState::INVALID: return JSIConverter<std::string>::toJSI(runtime, "invalid");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert PlayServicesState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("success"):
        case hashString("missing"):
        case hashString("updating"):
        case hashString("updateRequired"):
        case hashString("disabled"):
        case hashString("invalid"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
